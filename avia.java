public class avia extends transport {

    avia(String model, int year, double price, String color, Double power) {
        super(model, year, price, color, power);
    }
    avia istribitel = new avia(model, getYear(), getPeice(), model, null);
    avia vertic = new avia(model, year, year, model, null);
    
}
